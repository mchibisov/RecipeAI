# RecipeAI — Информационная система поиска рецептов с интеграцией LLM

Веб-приложение для авторизованного поиска кулинарных рецептов по базе данных с возможностью генерации интерактивных ответов, кулинарных советов и модификации инструкций искусственным интеллектом в режиме реального времени.

## Технологический стек
* **Backend:** Python 3.11+, Flask (WSGI-фреймворк), SQLAlchemy (ORM)
* **Frontend:** HTML5, CSS3, Vanilla JavaScript (Asynchronous Fetch API, DOM API)
* **Database:** PostgreSQL (хранение учетных записей пользователей и активных сессий)
* **AI Integration:** Mistral AI API (модель для генерации ответов по контексту рецепта)

## Архитектура и безопасность
* **Аутентификация:** Сессионная на базе токенов. Токены генерируются на стороне бэкенда и сохраняются в клиентских куках с флагами безопасности `HttpOnly` (защита от XSS) и `SameSite=Lax` (базовая защита от CSRF).
* **Безопасность данных:** Пароли пользователей хэшируются перед сохранением в базу данных (clear-text пароли не хранятся).
* **Frontend Security:** Реализовано клиентское экранирование HTML-сущностей (`escapeHtml`) для предотвращения уязвимостей типа Stored/Reflected XSS при обработке данных из внешних API.

## Локальное развертывание
1. Установите зависимости:
   ```bash
   pip install -r requirements.txt

   ```

2. Настройте конфигурационные переменные в файле `.env` (API-ключи, строка подключения к БД).
3. Запустите приложение:
```bash
python app.py
```

---

## Шаг 3: Куда залить проект (Хостинг)

Для Python/Flask приложений сейчас есть три оптимальных бесплатных (или с триал-периодом) международных хостинга, которые разворачивают проект напрямую из твоего GitHub-репозитория за 2 минуты:

1. **Render (render.com)** — Самый простой и популярный. Нажимаешь "New Web Service", привязываешь этот GitHub репозиторий, выбираешь среду `Python`, а в поле Start Command пишешь: `python app.py`. Базу данных PostgreSQL можно создать там же в один клик.
2. **Railway (railway.app)** — Очень быстрый, автоматически подхватывает проект, выдает бесплатный домен и легко разворачивает связку Flask + Postgres.
3. **Koyeb (koyeb.com)** — Современная альтернатива Render с отличным бесплатным тарифом для студенческих проектов.

### Твой план действий прямо сейчас:
1. Отредактируй функцию `run()` в `app.py` на локальном компьютере, как показано в Шаге 1.
2. Создай файл `README.md` с текстом из Шага 2.
3. Сделай коммит и пуш в репозиторий:
   ```bash
   git add .
   git commit -m "Prepare for deploy: update run config and add README.md"
   git push origin main

   ```

4. Выбери **Render.com**, зарегистрируйся через GitHub и попробуй развернуть. Если на каком-то этапе хостинга вылетит ошибка в логах — скидывай её сюда, сразу разберем причину.
